# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

정적 HTML. Python 스크립트(`publish.py`)가 디렉터리를 훑어 목차 HTML을 통째로 생성하고,
GitHub Pages(`docs/`)가 서빙한다. 빌드 단계·번들러·프레임워크 없음.
사용자가 못 박은 것: Pretendard, Tailwind(play CDN).

## Users

초등학교 교사들로 이루어진 **비공개 연구회** 구성원. 서로 아는 사이라 이곳이 무엇인지
설명할 필요가 없다. 격주 월요일 모임을 전후로 **교실 PC 앞에서 차분히** 연다.
모바일 급히 훑기가 아니라 큰 화면에서 앉아 읽는 장면이다.

방문 목적은 둘이 비슷한 비중이다: (1) 이번 회차에 읽을 것 열기, (2) 지난 회차에 쌓인 것을
되짚어 찾기. 최신호 전용 페이지도, 순수 아카이브도 아니다.

## Product Purpose

연구회가 함께 읽을 논문을 **그림 위주로 풀어 쓴 한 장짜리 설명**으로 바꿔 공유한다.
이 목차 페이지는 그 글들로 들어가는 입구다. 성공은 "모임 전에 각자 읽고 왔다"이다.

## Positioning

읽을거리는 원논문이 아니라 원논문을 **전문용어 없이 다시 쓴 글**이다. 각 글은 자체 SVG
그림 3~5개를 품고 있고, 저마다 다른 색·글꼴로 디자인돼 있다(생성 단계에서 논문 주제에서
끌어낸 팔레트를 쓴다). 목차는 그 이질적인 문서들을 하나로 묶는 유일한 공통면이다.

## Operating Context

- 격주 월요일 간격으로 회차가 쌓인다. 한 회차에 1~2편.
- 개인 정독 노트(`eli5/`)는 비공개이고, 그중 **고른 것만** 이 페이지로 공개된다.
- 게시는 수동이다: `python publish.py --date <날짜> <파일들>` → 목차 자동 재생성 → git push.
- 같은 저장소가 주간 논문 브리핑(`docs/`)과 이론 모음(`docs/theories/`)도 서빙한다.
  이 목차는 그중 연구회용 한 갈래다.

## Capabilities and Constraints

- 목차는 **파일 시스템이 곧 데이터**다. `docs/study/<날짜>/*.html`을 훑어 만든다.
  별도 DB·JSON 인덱스 없음.
- 각 항목의 제목과 한 줄 소개는 대상 HTML에서 `<h1>`과 `.callout`을 읽어 뽑는다.
  그 두 가지 외의 메타데이터(저자, 원논문 출처, 분량)는 **현재 없다**.
- 검색·태그·필터 기능 없음. 정렬은 날짜 역순 고정.
- 빌드 파이프라인이 없으므로 CSS 전처리·PostCSS·번들 불가. CDN과 인라인만 가능.

## Brand Commitments

- 글꼴: **Pretendard** (사용자 지정)
- CSS: **Tailwind** (사용자 지정)
- 이름: "연구회 논문 읽기"

## Evidence on Hand

- 실제 게시물 2편 (`docs/study/2026-09-22/`): KERIS 디지털교육 글로벌 동향 리포트 정리,
  교실 속 AI 윤리 위험 15가지 체계적 문헌고찰.
- 각 글은 본문 13,000~16,000자에 SVG 그림 4개.
- **지어내면 안 되는 것**: 구성원 수, 연구회 이름·소속, 모임 이력, 외부 인용·추천사.
  현재 확인된 것은 "격주 월요일"과 "초등 컴퓨터교육·AI교육"뿐이다.

## Product Principles

1. **입구는 글보다 조용해야 한다.** 각 글이 저마다 강한 색과 그림을 갖고 있으므로,
   목차가 화려하면 충돌한다. 목차의 일은 고르게 하는 것이다.
2. **최신과 과거를 동등하게 대한다.** 방문 목적이 반반이므로 최신호만 크게 키우거나
   과거를 접어두는 구조는 실패다.
3. **큰 화면을 전제로 한다.** 교실 PC에서 차분히 읽는 장면이므로 정보 밀도를 낮출
   이유가 없다. 모바일은 대응하되 기준이 아니다.
4. **설명하지 않는다.** 아는 사람만 오는 비공개 페이지다. 서비스 소개·온보딩·가치 제안
   문구는 군더더기다.
5. **없는 메타데이터를 지어내지 않는다.** 제목과 한 줄 소개가 가진 전부다.
   그 둘로 고를 수 있게 만드는 것이 설계 문제다.
